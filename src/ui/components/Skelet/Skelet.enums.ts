export enum SkeletVariants {
  Rectangular = 'rectangular',
  Circle = 'circle',
  Card = 'card',
  Cards = 'cards',
  List = 'list',
  TextList = 'textList',
  Table = 'table',
  Kanban = 'kanban',
  FilterBar = 'filterBar',
}
