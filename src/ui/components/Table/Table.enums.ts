export enum TableSeparators {
  halfLine = 'halfLine',
  line = 'line',
}

export enum TableCellComponents {
  Title = 'title',
  Count = 'count',
  Price = 'price',
  Chips = 'chips',
  Date = 'date',
  File = 'file',
  Phone = 'phone',
  Email = 'email',
  Select = 'select',
  Custom = 'custom',
  ViewAction = 'viewAction',
  EditAction = 'editAction',
  DeleteAction = 'deleteAction',
  CallAction = 'callAction',
  EmailAction = 'emailAction',
  MoreAction = 'moreAction',
  CustomAction = 'customAction',
}
